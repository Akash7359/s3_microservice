# s3_microservice
Layered **FastAPI** backend for file uploads to **AWS S3** with metadata persisted in **MySQL** via **SQLAlchemy**. Follows a clean Router → Service → Repository architecture. Includes S3 key structuring, file-size validation, Alembic migrations, and structured data modeling. Designed for scalability production-ready extensibility.
